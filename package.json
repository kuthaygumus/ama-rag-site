{
  "name": "ama-rag-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "The RAG day, step by step: why RAG, the architecture, a pipeline built stage by stage, and how to judge it for accuracy, security, performance and cost.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-sync.mjs && node scripts/check-legacy.mjs && astro build && node scripts/check-legacy.mjs dist && node scripts/check-dist.mjs",
    "preview": "astro preview",
    "sync": "node scripts/sync.mjs",
    "check:sync": "node scripts/check-sync.mjs",
    "check:legacy": "node scripts/check-legacy.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "@fontsource/caveat": "^5.2.0",
    "astro": "^7.3.1",
    "sharp": "^0.35.4"
  }
}
